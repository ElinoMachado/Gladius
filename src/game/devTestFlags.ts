/**
 * Modo de teste para UI (muitos artefatos / muitos inimigos).
 * `true` aplica artefatos extra e wave 1 especial; `false` = jogo normal.
 */
export const DEV_TEST_CROWD_UI = false;

/** Quantos artefatos do pool aplicar por herói em modo teste (fixo 48). */
export const DEV_TEST_ARTIFACT_COUNT = 48;

/** Inimigos extra na wave 1 em modo teste. */
export const DEV_TEST_WAVE1_ENEMY_COUNT = 48;
