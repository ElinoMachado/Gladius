# Dependencies
node_modules/

# Build
dist/

# Local / secrets
.env
.env.*
!.env.example

# Logs & OS
*.log
.DS_Store
Thumbs.db

# IDE (opcional — remove a linha se quiseres partilhar definições)
.idea/

# Test / coverage
coverage/

# Áudio “crowd” não referenciado pelo jogo (só menu + combate em /public/audio/*.mp3).
# Mantém o repo leve; o pack pode voltar a ser extraído localmente se precisares.
public/audio/_crowd_unpack/
public/audio/_crowd_unpack2/
public/audio/_crowd_pack.zip
